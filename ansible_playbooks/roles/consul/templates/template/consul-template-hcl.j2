consul {
    address = "{{ consul_servers | first }}:{{ consul_port }}"
    retry {
        enabled = true
        attempts = 12
        backoff = "250ms"
        max_backoff = "1m"
    }
}

wait {
    min = "30s"
    max = "120s"
}

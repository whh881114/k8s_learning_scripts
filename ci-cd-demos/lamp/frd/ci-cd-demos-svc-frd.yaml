apiVersion: v1
kind: Service
metadata:
  name: lamp-frd
  namespace: ci-cd-demos
  labels:
    name: lamp-frd
spec:
  type: ClusterIP
  clusterIP: 10.106.0.81
  ports:
    - port: 80        # 服务访问端口
      targetPort: 80  # 容器端口
  selector:
    name: lamp-frd
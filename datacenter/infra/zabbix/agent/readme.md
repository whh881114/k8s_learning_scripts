# zabbix-agent不适合使用daemonset部署在k8s上，主要理由如下：

- zabbix-agent一般都要部署些scripts，这些脚本有可能几个或是几十个都是有可能的，那么就要在configmap里定义这些文件，一个文件里
维护这么多脚本，显然不是那么合适。

- 当我们自定义监控脚本时，有可能会使用到`ss`或是`netstat`之类的命令，但是官方的镜像中是没有这样的命令的，所以还要重新制作镜像，
这个时间成本还是有点高。
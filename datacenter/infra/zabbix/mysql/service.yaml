apiVersion: v1
kind: Service
metadata:
  name: mysql-zabbix
  namespace: mysql
  labels:
    name: mysql-zabbix
spec:
  type: NodePort
  clusterIP: 10.98.126.191
  ports:
    - port: 3306        # 服务访问端口
      targetPort: 3306  # 容器端口
  selector:
    name: mysql-zabbix
kind: Deployment
apiVersion: apps/v1
metadata:
  name: public-infra
  namespace: nfs-client-provisioner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: public-infra
  template:
    metadata:
      labels:
        app: public-infra
    spec:
      serviceAccountName: nfs-client-provisioner-public-infra
      containers:
        - name: public-infra-ncp
          image: registry.freedom.org/quay.io/external_storage/nfs-client-provisioner:v3.1.0-k8s1.11
          volumeMounts:
            - name: nfs-client-root
              mountPath: /persistentvolumes
          env:
            - name: PROVISIONER_NAME
              value: fuseim.pri/ifs-public-infra
            - name: NFS_SERVER
              value: nfs.freedom.org
            - name: NFS_PATH
              value: /data01/k8s_nfs_pvc/public-infra
      volumes:
        - name: nfs-client-root
          nfs:
            server: nfs.freedom.org
            path: /data01/k8s_nfs_pvc/public-infra

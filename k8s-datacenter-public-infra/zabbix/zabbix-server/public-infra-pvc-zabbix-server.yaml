apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alertscripts-zabbix-server
  namespace: zabbix
  labels:
    name: alertscripts-zabbix-server
  annotations:
    volume.beta.kubernetes.io/storage-class: nfs-storage-public-infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: externalscripts-zabbix-server
  namespace: zabbix
  labels:
    name: externalscripts-zabbix-server
  annotations:
    volume.beta.kubernetes.io/storage-class: nfs-storage-public-infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: include-zabbix-server-agent
  namespace: zabbix
  labels:
    name: include-zabbix-server-agent
  annotations:
    volume.beta.kubernetes.io/storage-class: nfs-storage-public-infra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
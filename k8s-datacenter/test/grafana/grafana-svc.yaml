---
kind: Service
apiVersion: v1
metadata:
  labels:
    app: grafana
  name: grafana
  namespace: k8s-infra
spec:
  type: ClusterIP
  clusterIP: 10.106.99.2
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: grafana
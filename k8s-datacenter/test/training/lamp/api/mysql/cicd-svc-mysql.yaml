apiVersion: v1
kind: Service
metadata:
  name: lamp-mysql
  namespace: cicd
  labels:
    name: lamp-mysql
spec:
  type: ClusterIP
  clusterIP: 10.106.0.79
  ports:
    - port: 3306        # 服务访问端口
      targetPort: 3306  # 容器端口
  selector:
    name: lamp-mysql
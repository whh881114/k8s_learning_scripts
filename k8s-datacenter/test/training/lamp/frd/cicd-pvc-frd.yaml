apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lamp-frd-logs
  namespace: cicd
  labels:
    name: lamp-frd-logs
  annotations:
    volume.beta.kubernetes.io/storage-class: "cicd-nfs-storage"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
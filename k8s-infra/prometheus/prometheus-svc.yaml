---
kind: Service
apiVersion: v1
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  labels:
    app: prometheus
  name: prometheus
  namespace: k8s-infra
spec:
  type: ClusterIP
  clusterIP: 10.106.99.1
  ports:
    - port: 9090
      targetPort: 9090
  selector:
    app: prometheus